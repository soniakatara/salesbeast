# Database (SQLite)
DATABASE_URL="file:./dev.db"

# Auth (NextAuth v5 / Auth.js – generate with: openssl rand -base64 32)
AUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Optional: OpenAI for live coach (omit for mock mode)
# OPENAI_API_KEY="sk-..."
# OPENAI_MODEL="gpt-4o-mini"
